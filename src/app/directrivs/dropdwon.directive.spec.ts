import { DropdwonDirective } from './dropdwon.directive';

describe('DropdwonDirective', () => {
  it('should create an instance', () => {
    const directive = new DropdwonDirective();
    expect(directive).toBeTruthy();
  });
});
